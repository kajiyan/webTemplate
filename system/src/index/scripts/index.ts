/*
 * Event Listen Example
 */
// window.SW.eventEmitter.on('orientationchange', e => {});
// window.SW.eventEmitter.on('resize', e => {});
// window.SW.eventEmitter.on('scroll', e => {});
// window.SW.eventEmitter.on('endScroll', e => {});
// window.SW.eventEmitter.on('update', e => {});
// window.SW.eventEmitter.on('wheel', e => {});
// window.SW.eventEmitter.on('endWheel', e => {});

(() => {
  document.addEventListener('DOMContentLoaded', () => {
    console.log('DOM fully loaded and parsed');
  });
})();
